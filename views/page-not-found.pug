extends layout.pug

block content

  nav.nav
    a(href='/')
      span &larr;
      |  Back

    h1= `${error.message}`
    h2= `Please contact the site administror at help@fakeemail.com with error code: ${error.status}.`