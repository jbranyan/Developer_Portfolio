extends layout.pug

block content

  nav.nav
    a(href='/')
      span &larr;
      |  Back
    //- Displays Error message for when a global error is triggered
    h1= `${error.message}`
    h2= `Please contact the site administror at help@fakeemail.com with error code: ${error.status} or use back to return to the previous page`